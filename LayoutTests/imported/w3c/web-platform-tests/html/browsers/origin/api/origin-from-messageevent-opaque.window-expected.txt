
PASS Origin.from(MessageEvent) returns an opaque origin for a sandboxed frame.
FAIL Origin.from(MessageEvent) returns the same opaque origin for each message from a sandboxed frame. assert_true: expected true got false
PASS Origin.from(MessageEvent) returns distinct opaque origins for each message from a reloaded sandboxed frame.

